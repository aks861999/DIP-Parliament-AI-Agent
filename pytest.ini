[pytest]
markers =
    unit: fast, isolated tests with mocked dependencies — run on every push
    integration: tests hitting a real Postgres/Redis
    integration_live: tests hitting the real DIP API (needs DIP_API_KEY) — non-blocking in CI
asyncio_mode = auto
testpaths = tests